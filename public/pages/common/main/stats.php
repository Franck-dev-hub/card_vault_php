<?php
// Define variables
$pageTitle = "Stats";
$profileText = "Profile";
$contentFile = __FILE__;

if (!isset($isTemplate)):
    $isTemplate = true; include "public/pages/common/template.php";
else:
    // HTML content
endif; ?>