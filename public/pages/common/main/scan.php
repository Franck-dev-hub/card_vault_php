<?php
// Define variables
$pageTitle = "Scan";
$profileText = "Profile";
$contentFile = __FILE__;

if (!isset($isTemplate)):
    $isTemplate = true; include "public/pages/common/template.php";
else:
    // HTML content
endif; ?>